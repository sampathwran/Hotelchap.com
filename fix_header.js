const fs = require("fs"); let code = fs.readFileSync("src/components/Header.tsx", "utf-8"); code = code.replace("import { Globe, CircleDollarSign } from \"lucide-react\";", "import { Globe, CircleDollarSign } from \"lucide-react\";\nimport CurrencyModal from \"./CurrencyModal\";"); code = code.replace("onSelect={(c)", "onSelect={(c: string)"); fs.writeFileSync("src/components/Header.tsx", code);
